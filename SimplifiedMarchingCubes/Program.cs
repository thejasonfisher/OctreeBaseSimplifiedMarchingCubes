using System;
using System.Collections.Generic;
using System.Text;
using BaseClasses;

namespace SimplifiedMarchingCubes
{
    class Program
    {
        static void Main(string[] args)
        {
            //BitMap3d image = Sample.CreateSampleForLobster();
            //Mesh m = (new AdSMCProcessor(image)).GenerateSurface();
            //Console.WriteLine(m.Faces.Count);
            //PlyManager.Output(m, "test.ply");
        }
    }
}
